name: back_end
description: A server app using the shelf package and Docker.
version: 1.0.0

environment:
  sdk: ^3.5.4

dependencies:
  args: ^2.4.0
  shelf: ^1.4.2
  shelf_router: ^1.1.0
  localstore: ^1.4.0
  dotenv: ^4.2.0

dev_dependencies:
  http: ^1.1.0
  lints: ^4.0.0
  test: ^1.24.0